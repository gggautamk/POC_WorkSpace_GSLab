import {PersonExport} from './person';
import * as _ from 'lodash';

let obj = new PersonExport("FirstName", "Lastname");
console.log(obj);

var array = [1, 2, 3, 4, 5 ];
console.log(_.reverse(array));